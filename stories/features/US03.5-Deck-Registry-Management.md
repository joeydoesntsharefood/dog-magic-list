# US03.5: Deck Registry Management - Busca, Limpeza e Basic Land Exception

**Descrição:** Implementar ferramentas de filtragem e limpeza no grid do deck e ajustar a regra de Singleton para permitir múltiplos terrenos básicos em qualquer formato.

## Critérios de Aceite (AC):

### 1. Busca no Deck (Local Filter)
*   Adicionar um input de busca acima do `CURRENT_DECK_REGISTRY`.
*   O filtro deve ser em tempo real, ocultando cartas que não correspondam ao texto digitado (Nome ou Categoria).

### 2. Limpeza de Violações (Auto-Cleanup)
*   Adicionar o botão `[PURGE_VIOLATIONS]`.
*   Ao clicar, o sistema deve remover automaticamente todas as cartas que possuem `isLegal: false` no contexto do deck atual.

### 3. Exceção de Terrenos Básicos (Singleton Fix)
*   As regras de **Singleton (Commander)** e **Limite de 4 (Standard/Pauper)** não devem ser aplicadas a:
    *   `Plains`, `Island`, `Swamp`, `Mountain`, `Forest` (e suas versões em outras línguas como `Ilha`, `Pântano`, etc).
    *   Cartas com a propriedade `type_line` contendo `Basic Land`.

## Estratégia Técnica:
*   **React:** Novo estado `deckFilter` para gerenciar a visualização do grid.
*   **Lógica:** Refinar a função `validateCardLegality` para retornar `isLegal: true` sempre que a carta for um terreno básico.

## Metodologia TDI:
*   **Teste de Lógica:** Garantir que adicionar 33 Mountains no Commander não dispare violação.
*   **Teste de UI:** Validar se o botão `[PURGE_VIOLATIONS]` limpa o estado do `wizardDeck`.
